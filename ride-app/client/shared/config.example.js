// Lifeline — Client Configuration EXAMPLE
// Copy this file to config.js and fill in your real API keys.
// config.js is gitignored and must never be committed.

window.LIFELINE_CONFIG = {
  googleMapsApiKey: "YOUR_GOOGLE_MAPS_API_KEY"
};
